apiVersion: v1
kind: Pod
metadata: 
  name: service-demo
  # These labels are used to register this Pod with service
  labels:  
    project: roboshop
    component: frontend
    environment: dev
    purpose: node-port
spec: 
  containers:
    - name: nginx
      image: nginx
